# Models Directory

This directory is intended for locally downloaded or cached model weights.
Contents in this directory are gitignored to prevent committing large binary weight files to version control.
